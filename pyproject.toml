[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "design"
version = "0.0.1"
description = "Custom ERPNext App"
authors = [
    { name = "Precihole", email = "erpadmin@preciholesports.com" }
]
dependencies = []
